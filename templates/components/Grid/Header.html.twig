{# En-tête : caché sur mobile, grille sur desktop #}
<div role="rowgroup" {{ attributes.defaults({'class': 'hidden lg:grid lg:grid-cols-12 gap-4 px-6 py-3 bg-slate-50/80 border-b border-slate-200 text-slate-500 font-semibold text-xs uppercase tracking-wider'}) }}>
    {% block content %}{% endblock %}
</div>

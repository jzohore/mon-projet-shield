framework:
    rate_limiter:
        # Anti-abus du lien public d'accusé de réception du DER (par IP).
        der_acknowledgement:
            policy: 'sliding_window'
            limit: 10
            interval: '1 hour'
